

$('.current_week').on('click', function{
  $.get('/employees/current_employee.id/shifts', {
    type: 'get', 
    success: function(response){
      alert("Ive been clicked!");
    };
    data: {"tuesday": $('#t').data("tuesday")}
  });
});

$('#w').on('click', function{
  $.get('/employees/@shift.employee_id/shifts', {
    type: 'get',
    success: function(response){
      alert(null);
    };
    data: {"wedsnesday": $('#w').data("wedsnesday")}
  });
});

$('#th').on('click', function{
  $.get('/employees/@shift.employee_id/shifts', {
    type: 'get'.
    success: function(response){
      alert(null);
    };
    data: {"thursday": $('#th').data("thursday")}
  });
});

$('#f').on('click', function{ 
  $.get('/employees/@shift.employee_id/shifts', {
    type: 'get',
    success: function(response){ 
      alert(null);
    };
    data: {"friday": $('#f').data("friday")}
  });
});

$('#s').on('click', function{
  $.get('/employees/@shift.employee_id/shifts', {
    type: 'get',
    success: function(response){ 
      alert(null);
    };
    data: {"saturday": $('#s').data("saturday")}
   });
 });
}); 

$('#su').on('click', function{
  $.get('/employees/@shift.employee_id/shifts', {
    type: 'get',
    success: function(response){ 
      alert(null);
    };
    data: {"sunday": $('#su').data("sunday")}
  });
})
});
